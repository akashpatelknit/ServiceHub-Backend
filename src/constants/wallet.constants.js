export const WALLET_STATUSES = Object.freeze({
  ACTIVE: 'active',
  BLOCKED: 'blocked',
  DELETED: 'deleted',
});

export const WALLET_TYPES = Object.freeze({
  BANK: 'bank',
  CARD: 'card',
  WALLET: 'wallet',
});
