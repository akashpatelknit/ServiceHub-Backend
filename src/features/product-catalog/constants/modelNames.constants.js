// "Category" (legacy, src/models/category.model.js) and "CatalogCategory"
// (features/service-catalog) are both already taken — this is a third, deliberately
// separate category system scoped to products only (see ProductCategory model).
export const MODEL_NAMES = {
  PRODUCT_CATEGORY: 'ProductCategory',
};
